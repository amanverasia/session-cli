#!/bin/bash
# Session Controller CLI wrapper
# Usage: ./session-ctl <command> [args]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/.venv/bin/activate"
python "$SCRIPT_DIR/cli.py" "$@"
